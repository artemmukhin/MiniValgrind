#begin

// Max of two integers using pointers

ptr max(ptr x, ptr y) {
    if (*x > *y) {
        return x;
    }
    return y;
}

int main() {
    int x = 2;
    int y = 4;
    ptr m = max(&x, &y);
    print(*m); // "4"
    return 0;
}
#end